// Project Kappa — Background Service Worker
// Handles communication between content script and backend

chrome.runtime.onInstalled.addListener(() => {
  console.log('Project Kappa — Sovereignty Shield installed.');
});